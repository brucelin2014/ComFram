<Window x:Class="ComFram.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        WindowStartupLocation="CenterScreen"
        FontSize="20" Width="1080" Height="800" >

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="158*" />
            <ColumnDefinition Width="603*" />
        </Grid.ColumnDefinitions>

        <ListBox x:Name="Items"
                 DisplayMemberPath="DisplayName">
        </ListBox>
        <ContentControl Grid.Column="1" x:Name="ActiveItem" />

        <TextBlock x:Name="Data1"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   FontSize="20" />

        <Button x:Name="ClickMeShowWindow"
            Content="点我显示新窗体" HorizontalAlignment="Left" Margin="32,156,0,0" VerticalAlignment="Top" Width="150"/>

        <Button x:Name="PersonManage"
            Content="Person" HorizontalAlignment="Left" Margin="32,200,0,0" VerticalAlignment="Top" Width="150"/>

        <Button x:Name="UserManage"
            Content="User" HorizontalAlignment="Left" Margin="32,250,0,0" VerticalAlignment="Top" Width="150"/>

        <Button x:Name="Login"
            Content="Login" HorizontalAlignment="Left" Margin="32,300,0,0" VerticalAlignment="Top" Width="150"/>


    </Grid>

</Window>